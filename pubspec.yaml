name: audiobook_record
description: Audio Book recorder.
publish_to: 'none'
version: 0.2.2

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  record: ^5.1.2
  just_audio: ^0.9.41
  path_provider: ^2.1.4
  path: ^1.9.0
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  audioplayers: ^6.1.0
  flutter_screenutil: ^5.9.3

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/

