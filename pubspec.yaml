name: audiobook_record
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  record: ^5.1.2
  just_audio: ^0.9.41
  path_provider: ^2.1.4
  path: ^1.9.0
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  audioplayers: ^6.1.0
  sqflite: ^2.4.0

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/

